// metodo splice

// pop para simular usamos nomes.splice(1, -1)
// shift para simular usamos nomes.splice(0, 1)
// push para simular usamos nomes.splice(nomes.lenght, 0,'guilherme')
// unshift para simular usamos nomes.splice(0, 0, 'Evelin')