// ------ const == constante seria uma variavel imutavel que não pode ser alterada ao logo do código

// mesmas atribuições das variaveis
// unica coisa diferente é que ela precisa ser criada e já atribuida
// não se pode mudar o valor da constante
const nome = 'joão';
//console.log(nome)

const primeiroNumero = 5
const segundoNumero = 10
const resultado = primeiroNumero * segundoNumero
//console.log(resultado*3)
console.log(typeof primeiroNumero); // -> para saber o tipo 
