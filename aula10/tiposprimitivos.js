/* ------ tipos primitivos */
const nome = 'guilherme' // strings 

const n1 = 55 // numbers 

let nomeAluno; // undefined ->  == não aponta pra lugar nehum na memoria não é igual a null

let sobreNome = null /* -> Nulo == não aponta pra lugar nehum na memoria não é igual a undefined: 
                        usado quando os programadores querem desconfigurar alguma variavel, colocar
                        o valor da variavel como nulo*/ 

const aprovado = true // Boolean pode ser true==verdadeiro ou false==falso, só existe duas possibilidade de valor (valores lógicos)

console.log(typeof aprovado)