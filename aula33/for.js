const frutas = ['maçã', 'pêra', 'Uva', 'melancia', 'Goiaba', 'feira']


// i => index
for (let i = 0; i < frutas.length; i+=1) {
    // const par = i % 2 === 0 ? 'par' : 'impar'; 
    // console.log(i, par)
    console.log(`Indice ${i}`,frutas[i])
}